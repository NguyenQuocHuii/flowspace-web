/**
 * FlowSpace — Search Modal Component
 * Wrapper — logic đã nằm trong notifications.js (FS.searchModule)
 */
(function (FS) {
  'use strict';
  // FS.searchModule được định nghĩa trong notifications.js
  // File này để đảm bảo thứ tự load không gây lỗi
})(window.FS = window.FS || {});
